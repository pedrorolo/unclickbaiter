# elixir_buildback.config — pinned runtime versions for build/deploy
# Matches versions pinned in mise.toml

elixir_version=1.20.3
otp_version=29.0.5

# Reference (not used by Elixir buildpack) for parity with mise.toml
pipx_version=1.16.6
